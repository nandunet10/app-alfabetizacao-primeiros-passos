name: alfabetizacao_app
description: "Projeto de alfabetizacao infantil"

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.0.0
  flutter_tts: ^3.8.0
  google_fonts: ^6.0.0

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/audios/
    - assets/images/
    - assets/data/
